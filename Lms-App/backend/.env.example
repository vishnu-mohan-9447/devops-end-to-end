# Server
PORT=5000
NODE_ENV=development

# Database (dev/test: postgres container; prod: RDS endpoint)
DB_HOST=database
DB_PORT=5432
DB_NAME=lms_db
DB_USER=lms_user
DB_PASSWORD=lms_password

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=8h

# CORS
CORS_ORIGIN=http://localhost:8080
